{
  "servers": [
    {
      "name": "jira-clone-server",
      "url": "http://localhost:7888",
      "apiKey": "your_api_key_here",
      "description": "Jira MCP Server with project listing functionality",
      "tools": [
        {
          "name": "calculator",
          "description": "Performs basic arithmetic operations",
          "parameters": {
            "operation": "The operation to perform (add, subtract, multiply, divide)",
            "x": "First operand (number)",
            "y": "Second operand (number)"
          }
        },
        {
          "name": "list_jira_projects",
          "description": "Lists all available Jira projects"
        },
        {
          "name": "get_issue_details",
          "description": "Gets detailed information about a specific Jira issue",
          "parameters": {
            "issue_key": "The Jira issue key (e.g., 'PROJECT-123')"
          }
        },
        {
          "name": "get_weather",
          "description": "Gets simulated weather data for a location",
          "parameters": {
            "city": "City name",
            "country": "Optional country code"
          }
        }
      ],
      "resources": [
        {
          "uri": "resource://quotes",
          "description": "Returns a list of inspirational programming quotes"
        },
        {
          "uri": "resource://quotes/{index}",
          "description": "Returns a specific quote by index (0-3)",
          "parameters": {
            "index": "Quote index (integer)"
          }
        }
      ]
    }
  ]
}
