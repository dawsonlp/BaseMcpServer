Uses the new langchain 1.0 (in alpha at time of writing) to build some basic agents, including a jira agent that can run from the command line.

Please install the latest version of the alpha using pip install with tags. Use pyproject.toml and uv pip install -r pyproject.toml"
