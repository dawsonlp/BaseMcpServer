# Document Processor MCP Server Configuration
# Copy this file to .env and customize the values

# Server Configuration
SERVER_NAME=document-processor
HOST=0.0.0.0
PORT=7502
API_KEY=doc_processor_api_key

# Directory Configuration
OUTPUT_DIRECTORY=/app/output
INPUT_DIRECTORY=/app/input
HTML_TEMPLATE_DIR=/app/templates

# Document Processing Settings
PDF_ENGINE=weasyprint
MAX_FILE_SIZE_MB=50

# Available PDF engines: weasyprint, pdfkit, reportlab, pandoc
# weasyprint: Best for HTML to PDF conversion with CSS support
# pdfkit: Requires wkhtmltopdf, good for complex layouts
# reportlab: Basic PDF generation, good for simple documents
# pandoc: Universal document converter, requires pandoc installation
