# Requires Python 3.13+
# Core MCP dependencies
mcp>=1.14.0

# MCP Commons - shared infrastructure (provides PyYAML, bulk registration, server utilities)
mcp-commons>=1.0.0

# HTTP server dependencies for streamable transport
starlette>=0.41.0
uvicorn[standard]>=0.35.0

# Jira integration dependencies
atlassian-python-api>=4.0.7

# Graph visualization dependencies
graphviz>=0.21
networkx>=3.5

# Testing dependencies
pytest>=8.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
